import 'package:flutter/material.dart';

class SlidePreview extends StatelessWidget {
  const SlidePreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('`SlidePreview` has not yet been implemented!'),
    );
  }
}
